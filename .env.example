# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:3000/api

# Set to "true" to use local mock data instead of hitting the API
# Useful during frontend development before the backend is ready
VITE_USE_MOCK=true
